# دليل نقل وتثبيت واختبار البرنامج على Windows 7 (دليل ميداني)

هذا الدليل يوضح خطوات نقل برنامج **رفيق نقاط البيع** وتثبيته على أي جهاز خارجي يعمل بنظام **Windows 7 (32-bit أو 64-bit)** وحتى **Windows 11**.

---

## 1. أين تجد ملف التثبيت المجمع (Single Installer .exe)؟

تم تجميع المشروع بالكامل في ملف تثبيت واحد احترافي حجمه **~4.1 ميجابايت فقط**:
📍 **المسار على جهازك:**
```
RAFIQ\installer\Output\RafiqPOS_Setup_v1.0.0.exe
```
*(أو يمكنك إعادة بنائه بضغطة زر في أي وقت عبر تشغيل: `انشاء_ملف_التثبيت_Setup.bat`)*

---

## 2. ماذا يحدث عند تشغيل `RafiqPOS_Setup_v1.0.0.exe` على جهاز العميل؟

1. **واجهة عربية أنيقة وسريعة:**
   - يفتح معالج التثبيت (Wizard) باللغة العربية.
   - يسمح للعميل باختيار مسار التثبيت وعمل اختصار على سطح المكتب.
2. **فحص المتطلبات المسبقة تلقائياً (Prerequisites Check):**
   - يفحص هل حزمة **.NET Framework 4.8** مثبتة على الجهاز أم لا؛ وإذا لم تكن موجودة، تظهر رسالة عربية واضحة ترشده للحل بدلاً من ظهور أخطاء مبهمة.
   - يفحص مشغل **WebView2 Runtime** للتأكد من قدرة النظام على عرض الواجهة.
3. **حماية بيانات المحل (Data Safety):**
   - قاعدة البيانات `rafiq_pos.db` مصانة تماماً؛ إذا كان العميل يثبت تحديثاً فوق نسخة قديمة، فالمثبّت **لن يمس بيانات مبيعاته أو حسابات عملائه أبداً** بفضل خاصية `onlyifdoesntexist` و `uninsneveruninstall`.
4. **تثبيت ملف إلغاء التثبيت النظيف (Clean Uninstaller).**

---

## 3. المتطلبات المسبقة لأجهزة Windows 7 القديمة (Clean Win7 Setup)

إذا كان الجهاز المستهدف عليه **Windows 7 خام/قديم جداً**، فإليك ما يحتاجه مرة واحدة في العمر:

| المتطلب | الوصف | الرابط / الأداة |
| :--- | :--- | :--- |
| **Windows 7 Service Pack 1** | الحزمة الخدمية الأولى (شرط إلزامي لأي برنامج دوت نت حديث) | مدمجة في أغلب نسخ ويندوز 7 |
| **تحديثات توقيع SHA-2** | تحديثين من مايكروسوفت: `KB4490628` و `KB4474419` | متاحان مجاناً من Microsoft Update Catalog |
| **.NET Framework 4.8** | المثبّت غير المتصل (Offline Installer) | `ndp48-x86-x64-allos-enu.exe` |
| **WebView2 Fixed Version 109** | المشغل المجمد المخصص لويندوز 7 | يوضع في مجلد `runtimes\fixed109` داخل البرنامج |

> 💡 **ملاحظة لويندوز 10 وويندوز 11:** 
> على ويندوز 10 و 11، **لن تحتاج لأي شيء على الإطلاق**؛ فقط اضغط على `RafiqPOS_Setup_v1.0.0.exe` وستعمل فوراً لأن دوت نت 4.8 ومشغل WebView2 مدمجان مسبقاً في النظام.

---

## 4. قائمة فحص الاختبار على جهاز ويندوز 7 (Checklist)

عند فتح البرنامج على جهاز ويندوز 7:
- [ ] التأكد من إقلاع نافذة البرنامج وظهور واجهة رفيق.
- [ ] الضغط على زر **«قياس زمن الاستجابة (Ping)»** للتأكد من سلامة جسر الـ IPC وسرعته.
- [ ] الضغط على زر **«تنفيذ معاملة ذرية تجريبية»** للتأكد من اتصال قاعدة بيانات SQLite وكتابة 10 أصناف.
- [ ] تجربة مسح أي باركود بقارئ الباركود والتأكد من التقاط الأرقام.
