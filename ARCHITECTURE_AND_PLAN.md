# وثيقة المعمارية الفنية وخطة التنفيذ الاحترافية (Senior Architecture & Execution Plan)
## مشروع نظام إدارة السوبرماركت ونقاط البيع (Desktop POS System)

---

## 1. الفحص التقني للبيئة الحالية على جهازك (Environment Status)

تم فحص بيئة العمل الحالية في نظام التشغيل Windows، وكانت النتائج كالتالي:

| التقنية / الأداة | الحالة الحالية | الإصدار المكتشف | التقييم الهندسي والجاهزية |
| :--- | :---: | :---: | :--- |
| **Node.js** | ✅ متاح ومثبت | `v24.14.1` | ممتاز جداً لبناء واجهة React / Vite وأدوات التطوير |
| **npm** | ✅ متاح ومثبت | `11.12.0` | ممتاز لإدارة الحزم والتبعيات |
| **Python** | ✅ متاح ومثبت | `3.13.15` | جاهز للأتمتة ومعالجة البيانات والاسكربتات |
| **Git** | ✅ متاح ومثبت | `2.55.0` | جاهز للـ Version Control وإدارة الفروع |
| **.NET Framework** | ⚠️ متوفر كـ Runtime | `v4.0.30319` (مع MSBuild & csc) | يمكنه تشغيل/بناء تطبيقات دوت نت 4.8 لكن يفضل توفير بيئة تطوير حديثة |
| **.NET SDK (dotnet CLI)** | ❌ غير مثبت | `No SDKs found` | نحتاجه إذا أردنا استخدام C# بأدوات دوت نت الحديثة (.NET 8 SDK) |
| **Rust / Cargo** | ❌ غير مثبت | غير متوفر | (غير مطلوب إلا لو تم اختيار Tauri) |

---

## 2. دراسة الخيارات التقنية وتوصية السينيور (Tech Stack Decision Matrix)

المشروع في الباك لوج وضع شرطاً حرجاً: **العمل على أجهزة المحلات القديمة (Windows 7 SP1 وما بعده) بنسخة واحدة 32/64 بت مع استهلاك رام منخفض جداً (RAM < 500MB)**.

بناءً على ذلك، إليك المقارنة المعمارية بين أفضل 3 حلول:

```
                  ┌──────────────────────────────────────────────────────────┐
                  │                 خيارات المعمارية التقنية                 │
                  └────────────────────────────┬─────────────────────────────┘
                                               │
           ┌───────────────────────────────────┼───────────────────────────────────┐
           ▼                                   ▼                                   ▼
  【 الخيار الأول: الموصى به 】         【 الخيار الثاني: البديل السريع 】     【 الخيار الثالث: الحديث 】
   C# (.NET 4.8 / Core)                  Electron (Fixed v22)                 .NET 8 WPF / Modern
   + WebView2 (Fixed 109)                + React / Vite                       + React / WebView2
   + React UI + SQLite                   + SQLite (better-sqlite3)            (مخصص لويندوز 10/11 فقط)
   ───────────────────────               ─────────────────────────            ───────────────────────
   • استهلاك رام: 120-180MB               • استهلاك رام: 280-400MB             • استهلاك رام: 150-200MB
   • دعم ويندوز 7 و 10 و 11              • دعم ويندوز 7 و 10 و 11             • يسقط ويندوز 7 بالكامل
   • تحكم خام في الطابعات والمنافذ       • تطوير سريع جداً بلغة واحدة JS/TS   • يحتاج تنزيل .NET 8 SDK
```

### التوصية الهندسية (Senior Recommendation):
1. **المسار الأساسي للمشروع (طبقاً للباك لوج):**
   - **Backend / Host:** مضيف C# (.NET Framework 4.8 لضمان ويندوز 7) مع **WebView2** (استخدام Fixed Version Runtime 109 للأجهزة القديمة، أو Evergreen لويندوز 10/11) + محرك **SQLite** محلي بوضع WAL.
   - **Frontend:** تطبيق **React 18 + TypeScript + Vite**، مع ضبط هدف البناء (Build Target) إلى `chrome109` لضمان التوافق المطلق، بتصميم **Tailwind/Vanilla CSS** يدعم RTL والشاشات الصغيرة (1024x768).
2. **المسار البديل شديد السرعة (Full-Stack TypeScript):**
   - **Electron v22** (آخر إصدار رسمي يدعم Windows 7 و 8.1 مع Chromium 108/109) + Node.js + SQLite (`better-sqlite3`) + React. هذا المسار ميزته أنه جاهز للبدء فوراً بالاعتماد على Node.js المتوفر لديك الآن دون الحاجة لتثبيت أي SDK إضافي.

---

## 3. الركائز المعمارية للنظام (Core Engineering Pillars)

لضمان أن يكون النظام مستقراً ولا ينهار في بيئة السوبرماركت الحقيقية:

### أ) الأمان المالي المطلق (Integer Financial Arithmetic)
- **منع استخدام الأرقام العشرية (Float/Double):** في لغات البرمجة، الأرقام العشرية تسبب أخطاء تقريب كارثية في الحسابات التراكمية.
- **القاعدة الصارمة:**
  - كل المبالغ المالية تُخزّن وتُحسب **بالقروش (Piasters)** كأعداد صحيحة `BIGINT` / `long` (مثال: 15.50 جنيه = `1550` قرش).
  - التحويل للجنيه يتم فقط في طبقة العرض للمستخدم (Presentation Layer).
  - الأوزان والكميات تُخزّن **بالجرام (Grams)** أو بأجزاء الألف كأعداد صحيحة.

### ب) الصمود ضد انقطاع الكهرباء الفجائي (Power-Cut Resilience & ACID)
- في السوبرماركت، قد تنطفئ الكهرباء فجأة أثناء كتابة فاتورة.
- تفعيل وضع **SQLite Write-Ahead Logging (WAL)**:
  ```sql
  PRAGMA journal_mode = WAL;
  PRAGMA synchronous = NORMAL;
  PRAGMA foreign_keys = ON;
  PRAGMA busy_timeout = 5000;
  ```
- **المعاملة الواحدة الذرية (Atomic Unit of Work):** حفظ الفاتورة، تفاصيل الأصناف، خصم المخزون، وتسجيل حركة الخزينة/العميل يتم داخل `BEGIN TRANSACTION ... COMMIT`. إما أن تُحفظ العملية بالكامل أو تُلغى بالكامل دون ترك أي بيانات تالفة.

### ج) معرّفات السجلات الفريدة (UUIDs vs Auto-Increment)
- استخدام **UUID v4 / GUID** لجميع المفاتيح الأساسية (`id`).
- **الفائدة:** سهولة إضافة فروع ومزامنة البيانات السحابية مستقبلاً دون حدوث تصادم معرّفات (ID Collisions).
- تخصيص حقل مستقل لرقم الفاتورة المتسلسل اليومي البسيط المقروء للكاشير والعميل (`invoice_number` متزايد وقابل للبحث).

### د) عزل طبقة الهاردوير والطرفيات (Hardware Abstraction Layer - HAL)
- **طابعات الإيصالات الحرارية (ESC/POS Thermal Printers):**
  - دعم الطباعة المباشرة عبر Raw Win32 Spooler / USB / Serial Port.
  - إرسال أوامر ESC/POS مباشرة مع دعم الحروف العربية المشبّكة بدون تشويه.
- **قارئ الباركود (Barcode Scanner Handling):**
  - معالجة الباركود القادم من قارئ الكيبورد (HID Keyboard Emulation) بالتقاط الفواصل الزمنية السريعة بين الضغطات، وتحويل أكواد المفاتيح الفعلية (Virtual Keycodes) بحيث لا يتأثر البرنامج إذا كانت لغة لوحة المفاتيح الحالية "عربي" أو "إنجليزي".

### هـ) نموذج المعمارية النظيفة (Clean Architecture)

```
┌─────────────────────────────────────────────────────────────┐
│                 واجهة المستخدم (React RTL UI)                │
│    شاشة البيع السريعة | إدارة المنتجات | التقارير والإقفال  │
└──────────────────────────────┬──────────────────────────────┘
                               │ IPC Bridge (Zod Validated Messages)
┌──────────────────────────────▼──────────────────────────────┐
│            طبقة التطبيق والخدمات (Application Services)      │
│  SaleService | InventoryService | CustomerDebt | BackupMgr  │
└──────────────────────────────┬──────────────────────────────┘
                               │ Domain Logic & Events
┌──────────────────────────────▼──────────────────────────────┐
│             نواة الأعمال وقواعد الدومين (Core Domain)        │
│    Money (Integer) | TaxEngine | InvoiceAggregate | Audit   │
└──────────────────────────────┬──────────────────────────────┘
                               │ Repository Pattern
┌──────────────────────────────▼──────────────────────────────┐
│            طبقة البنية التحتية (Infrastructure Layer)        │
│   SQLite (WAL Mode) | ESC/POS Printer Driver | File Storage │
└─────────────────────────────────────────────────────────────┘
```

---

## 4. خارطة طريق التنفيذ التدريجي (Phased Execution Roadmap)

المشروع مقسم إلى **12 محطة رئيسية متتالية** طبقاً للباك لوج الموثق في [`PROJECT_BACKLOG.md`](file:///c:/Users/khale/OneDrive/Desktop/RAFIQ/PROJECT_BACKLOG.md):

| المرحلة | المحطة | الأهداف الرئيسية | عدد المهام |
| :---: | :--- | :--- | :---: |
| **0** | **التحقق التقني (Spike)** | التحقق على ويندوز 7 و10، اختبار WebView2/Electron، اختبار انقطاع الكهرباء وقاعدة SQLite، تجربة الطابعة وقارئ الباركود | 9 مهام |
| **1** | **الأساس التقني القوي** | قاعدة البيانات، الجداول، الهيكل، إدارة الأخطاء، دعم اللغتين، نظام الصلاحيات، قياس الأداء | 85 مهمة |
| **2** | **حماية البيانات والنسخ** | النسخ الاحتياطي التلقائي اليومي، فحص السلامة، الاسترجاع بضغطة واحدة | 18 مهمة |
| **3** | **المنتجات والمخزون الأساسي** | إضافة الأصناف، الباركودات المتعددة، أوزان الكيلو/الجرام، استيراد إكسل، حركات المخزن | 43 مهمة |
| **4** | **أول بيعة (Core POS)** | سلة البيع، اختصارات الكيبورد، الحساب النقدي، طباعة الإيصال الحراري، تعليق الفواتير | 59 مهمة |
| **5** | **التسليم التجريبي الآمن** | المثبت الآلي (Inno Setup)، معالج أول تشغيل، شاشة فحص الجاهزية والبيانات التجريبية | 33 مهمة |
| **6** | **العملاء والآجل** | كشف حساب العميل، البيع بالآجل، الدفعات، تنبيهات سقف الديون، استيراد الديون | 37 مهمة |
| **7** | **اكتمال البيع** | الخصومات، المرتجعات، إلغاء الفواتير مع أسباب الإلغاء | 22 مهمة |
| **8** | **المشتريات والموردين** | فواتير الشراء، حسابات الموردين، تسويات المخزون، حد الطلب | 16 مهمة |
| **9** | **التقارير والإقفال** | تقرير المبيعات والأرباح، قفل اليومية (Z-Report)، الأصناف الأكثر مبيعاً والراكدة | 19 مهمة |
| **10** | **الاعتماد النهائي للنسخة 1** | أداة نقل النظام لجهاز جديد، دليل التشغيل السريع وفيديوهات تدريبية | 7 مهام |
| **11** | **الإضافات والتوسع** | تواريخ الصلاحية والدفعات، الوحدات المتعددة (كرتونة/قطعة)، نظام التراخيص والتفعيل | 65 مهمة |

---

## 5. الخطوة التالية المقترحة للبدء فوراً

1. **اعتماد المكدس البرمجي (Tech Stack Choice):**
   - هل تفضل السير في مسار **C# (.NET 4.8 / .NET 8) + WebView2 + React** (يتطلب تثبيت .NET SDK للبيئة الحديثة)؟
   - أم مسار **Electron v22 + React + SQLite** (جاهز للعمل فوراً على أدوات جهازك الحالية دون أي تنزيلات خارجية)؟
2. **البدء بالمحطة 0 (Milestone 0: Technical Spike):**
   - تجهيز الهيكل الأساسي للنافذة وقاعدة البيانات وتجربة اتصال الـ IPC.
